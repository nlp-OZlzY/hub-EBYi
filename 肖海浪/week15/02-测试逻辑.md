# 多模态RAG聊天机器人 - 测试逻辑

## 一、测试环境依赖

| 依赖项 | 测试策略 | 说明 |
|--------|---------|------|
| SQLite | 使用内存数据库 | 测试隔离，不污染真实数据 |
| Milvus | Mock或使用测试Collection | 避免依赖线上Milvus |
| Kafka | Mock | 不依赖真实Kafka |
| mineru | Mock | 解析结果用预置的markdown文件 |
| bge模型 | Mock编码结果 | 返回固定向量，不加载真实模型 |
| clip模型 | Mock编码结果 | 返回固定向量，不加载真实模型 |
| Qwen-VL API | Mock | 返回预置的回答 |

---

## 二、测试目录结构

```
tests/
├── conftest.py              # 公共fixtures
├── test_unit/
│   ├── __init__.py
│   ├── test_text_split.py   # 文本切分测试
│   ├── test_encode.py       # 编码函数测试
│   └── test_orm.py          # ORM模型测试
├── test_api/
│   ├── __init__.py
│   ├── test_upload.py       # 上传接口测试
│   ├── test_chat.py         # 问答接口测试
│   └── test_files.py        # 文件管理接口测试
├── test_integration/
│   ├── __init__.py
│   └── test_full_flow.py    # 集成测试
└── mock_data/
    ├── sample.pdf           # 测试用PDF
    ├── sample.docx          # 测试用DOCX
    ├── sample.txt           # 测试用TXT
    └── mock_markdown.md     # mock mineru解析结果
```

---

## 三、测试用例描述

### 3.1 单元测试用例

#### 3.1.1 文本切分函数 split_text2chunks

| 用例ID | 输入 | 预期输出 | 说明 |
|--------|------|---------|------|
| UT-001 | [] | [] | 空输入返回空列表 |
| UT-002 | ["这是一段测试文本"] | ["这是一段测试文本"] | 单行短文本 |
| UT-003 | ["第一行", "第二行", "第三行"] | ["第一行\n第二行\n第三行"] | 多行合并 |
| UT-004 | ["a"*200, "b"*200, "c"*200] chunk_size=256 | 2个chunk | 超过chunk_size切分 |
| UT-005 | ["第一行", "", "第三行"] | ["第一行\n第三行"] | 跳过空行 |
| UT-006 | ["正文", "# References", "参考文献"] | ["正文"] | 跳过References标题 |
| UT-007 | ["正文", "[1] Author 2024", "[2] Paper"] | ["正文"] | 跳过引用行 |
| UT-008 | ["文本", "![图1](./img.png)", "更多文本"] | 包含图片行 | 保留图片引用 |

#### 3.1.2 编码函数 encode_text_and_image

| 用例ID | 输入 | 预期输出 | 说明 |
|--------|------|---------|------|
| UT-010 | 纯文本 | (bge_vec[512], clip_vec[1024], zeros[1024]) | 无图片时图片向量为0 |
| UT-011 | 含图片引用的文本 | (bge_vec[512], clip_vec[1024], image_vec[1024]) | 正确编码图片 |
| UT-012 | 图片路径拼接 | 验证路径正确拼接 | /data/docs/paper/vlm/content.md + ./images/fig1.png → /data/docs/paper/vlm/fig1.png |

#### 3.1.3 ORM模型 File

| 用例ID | 操作 | 预期结果 | 说明 |
|--------|------|---------|------|
| UT-020 | 创建File记录 | id不为None，字段值正确 | 基本CRUD |
| UT-021 | 查询File记录 | 返回正确的记录 | 基本CRUD |
| UT-022 | 更新filestate | 状态正确更新 | 状态流转 |
| UT-023 | 删除File记录 | 记录不存在 | 基本CRUD |
| UT-024 | 查询所有File | 返回所有记录 | 列表查询 |
| UT-025 | 状态流转：已上传→解析中→已解析 | 每步状态正确 | 完整流程 |

---

### 3.2 接口测试用例

#### 3.2.1 POST /upload/document

| 用例ID | 场景 | 请求 | 预期响应 |
|--------|------|------|---------|
| API-001 | 正常上传PDF | file=test.pdf | 200, 返回file_id |
| API-002 | 正常上传DOCX | file=test.docx | 200, 返回file_id |
| API-003 | 正常上传TXT | file=test.txt | 200, 返回file_id |
| API-004 | 不支持的文件类型 | file=test.exe | 400, 错误提示 |
| API-005 | 空文件 | file=空 | 400, 错误提示 |
| API-006 | 无文件 | 无file字段 | 422, 参数错误 |
| API-007 | 验证Kafka消息发送 | - | send被调用，参数正确 |
| API-008 | 验证UUID文件名 | - | 文件名使用UUID格式 |
| API-009 | 上传大文件(10MB) | - | 成功或返回413 |
| API-010 | 验证Kafka消息格式 | - | 包含file_name, file_path, id |

#### 3.2.2 POST /chat

| 用例ID | 场景 | 请求 | 预期响应 |
|--------|------|------|---------|
| API-020 | 正常提问 | question="什么是RAG？" | 200, 返回answer+sources |
| API-021 | 空问题 | question="" | 400, 错误提示 |
| API-022 | 无匹配结果 | question="完全无关xyz" | 200, sources为空 |
| API-023 | 验证检索参数 | - | limit=5, anns_field=text_vector |
| API-024 | 验证Prompt拼接 | - | 包含问题和检索资料 |
| API-025 | 验证bge编码调用 | - | encode被正确调用 |
| API-026 | 验证图片路径替换 | - | images/路径正确替换 |
| API-027 | 验证多条结果拼接 | - | 所有资料都包含在Prompt中 |
| API-028 | LLM调用异常处理 | - | 返回错误或默认回答 |

#### 3.2.3 GET /files

| 用例ID | 场景 | 预期响应 |
|--------|------|---------|
| API-040 | 无文件记录 | 200, 返回空列表 |
| API-041 | 有文件记录 | 200, 返回文件列表 |
| API-042 | 多个文件 | 200, 返回所有文件 |
| API-043 | 验证返回字段 | 包含id, filename, filepath, filestate |

#### 3.2.4 DELETE /files/{file_id}

| 用例ID | 场景 | 预期响应 |
|--------|------|---------|
| API-050 | 删除存在的文件 | 200, 删除成功 |
| API-051 | 删除不存在的文件 | 404, 文件不存在 |
| API-052 | 本地文件不存在时删除 | 200, 记录删除成功 |
| API-053 | 验证Milvus清理 | delete被调用，参数正确 |
| API-054 | 重复删除同一文件 | 第一次200，第二次404 |

---

### 3.3 集成测试用例

| 用例ID | 流程 | 验证点 |
|--------|------|--------|
| IT-001 | 上传→检索 完整流程 | 上传成功，能检索到内容 |
| IT-002 | 上传→删除→检索 | 删除后检索不到 |
| IT-003 | 多文件上传→跨文件检索 | sources包含多个文件 |
| IT-004 | 文件状态流转 | 已上传→解析中→已解析 |
| IT-005 | 并发上传10个文件 | 全部成功 |

---

## 四、可执行测试代码

### 4.1 conftest.py

```python
import pytest
import os
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from unittest.mock import Mock, patch

from main import app
from orm_model import Base


@pytest.fixture(autouse=True)
def setup_test_env(tmp_path):
    """每个测试用例独立的临时目录"""
    os.chdir(tmp_path)
    os.makedirs("uploads", exist_ok=True)
    os.makedirs("processed", exist_ok=True)
    yield


@pytest.fixture
def db_session():
    """内存SQLite，每个测试隔离"""
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    TestSession = sessionmaker(bind=engine)
    session = TestSession()
    yield session
    session.close()


@pytest.fixture
def client(db_session):
    """FastAPI测试客户端"""
    with TestClient(app) as c:
        yield c


@pytest.fixture
def mock_kafka():
    """Mock KafkaProducer"""
    with patch("main.KafkaProducer") as mock:
        producer_instance = Mock()
        mock.return_value = producer_instance
        yield producer_instance


@pytest.fixture
def mock_milvus():
    """Mock MilvusClient"""
    with patch("main.MilvusClient") as mock:
        client_instance = Mock()
        mock.return_value = client_instance
        yield client_instance


@pytest.fixture
def mock_bge_model():
    """Mock bge模型，返回固定向量"""
    with patch("main.bge_model") as mock:
        mock.encode.return_value = [0.1] * 512
        yield mock


@pytest.fixture
def mock_clip_model():
    """Mock clip模型，返回固定向量"""
    with patch("main.clip_model") as mock:
        mock.encode.return_value = [0.2] * 1024
        yield mock


@pytest.fixture
def mock_qwen_client():
    """Mock Qwen-VL客户端"""
    with patch("main.qwen_client") as mock:
        mock.chat.completions.create.return_value = Mock(
            choices=[Mock(message=Mock(content="这是mock的回答"))]
        )
        yield mock


@pytest.fixture
def sample_pdf():
    """测试用PDF文件"""
    return ("test.pdf", b"%PDF-1.4 fake pdf content", "application/pdf")


@pytest.fixture
def sample_docx():
    """测试用DOCX文件"""
    return ("test.docx", b"PK fake docx content", "application/vnd.openxmlformats-officedocument.wordprocessingml.document")


@pytest.fixture
def sample_txt():
    """测试用TXT文件"""
    return ("test.txt", b"Hello World\nThis is test content.", "text/plain")


@pytest.fixture
def mock_mineru_result(tmp_path):
    """Mock mineru解析结果目录"""
    doc_dir = tmp_path / "processed" / "test"
    doc_dir.mkdir(parents=True)
    images_dir = doc_dir / "vlm" / "images"
    images_dir.mkdir(parents=True)

    # 创建mock的markdown文件
    md_content = """# 测试文档

## 第一章 RAG概述

RAG（检索增强生成）是一种结合检索和生成的技术。

![RAG架构图](./images/rag_arch.png)

## 第二章 向量检索

向量检索是RAG的核心组件之一。
"""
    (doc_dir / "vlm" / "content.md").write_text(md_content, encoding="utf-8")

    # 创建mock图片
    (images_dir / "rag_arch.png").write_bytes(b"fake image bytes")

    return doc_dir
```

---

### 4.2 test_unit/test_text_split.py

```python
import pytest
from offline_precess_worker import split_text2chunks


class TestSplitText2Chunks:
    """split_text2chunks 函数测试"""

    def test_empty_input(self):
        """空输入返回空列表"""
        result = split_text2chunks([])
        assert result == []

    def test_single_short_line(self):
        """单行短文本"""
        result = split_text2chunks(["这是一段测试文本"])
        assert len(result) == 1
        assert result[0] == "这是一段测试文本"

    def test_multiple_lines_within_limit(self):
        """多行文本，总长度未超过chunk_size"""
        lines = ["第一行", "第二行", "第三行"]
        result = split_text2chunks(lines, chunk_size=100)
        assert len(result) == 1
        assert "第一行" in result[0]
        assert "第三行" in result[0]

    def test_multiple_chunks_when_exceed_limit(self):
        """超过chunk_size时切分为多个chunk"""
        lines = ["a" * 200, "b" * 200, "c" * 200]
        result = split_text2chunks(lines, chunk_size=256)
        assert len(result) >= 2

    def test_skip_empty_lines(self):
        """跳过空行"""
        lines = ["第一行", "", "", "第四行"]
        result = split_text2chunks(lines)
        assert len(result) == 1
        assert result[0] == "第一行\n第四行"

    def test_skip_references_title(self):
        """跳过 # References 标题"""
        lines = ["正文内容", "# References", "参考文献1", "参考文献2"]
        result = split_text2chunks(lines)
        assert len(result) == 1
        assert "References" not in result[0]
        assert "参考文献" not in result[0]

    def test_skip_citation_lines(self):
        """跳过 [1] xxx 引用行"""
        lines = [
            "正文内容",
            "[1] Author et al. Title. 2024",
            "[2] Another Author. Title. 2023",
            "更多正文"
        ]
        result = split_text2chunks(lines)
        assert len(result) == 1
        assert "[1]" not in result[0]
        assert "[2]" not in result[0]
        assert "正文内容" in result[0]
        assert "更多正文" in result[0]

    def test_keep_image_lines(self):
        """保留图片引用行"""
        lines = ["文本内容", "![图1](./images/fig1.png)", "更多文本"]
        result = split_text2chunks(lines)
        assert len(result) == 1
        assert "![" in result[0]
        assert "fig1.png" in result[0]

    def test_chunk_size_boundary(self):
        """chunk_size边界测试"""
        lines = ["a" * 256, "b" * 10]
        result = split_text2chunks(lines, chunk_size=256)
        assert len(result) >= 1

    def test_real_world_document(self):
        """真实文档场景模拟"""
        lines = [
            "# 论文标题",
            "",
            "## 摘要",
            "本文提出了一种新的方法...",
            "",
            "## 1. 引言",
            "深度学习在NLP领域取得了巨大成功[1]。",
            "",
            "![图1](./images/fig1.png)",
            "",
            "# References",
            "[1] Vaswani et al. Attention is all you need. 2017",
            "[2] Devlin et al. BERT. 2019"
        ]
        result = split_text2chunks(lines, chunk_size=200)
        full_text = "\n".join(result)
        assert "论文标题" in full_text
        assert "摘要" in full_text
        assert "![图1]" in full_text
        assert "References" not in full_text
        assert "[1] Vaswani" not in full_text
```

---

### 4.3 test_unit/test_encode.py

```python
import pytest
import numpy as np
from unittest.mock import Mock, patch, MagicMock
from offline_precess_worker import encode_text_and_image


class TestEncodeTextAndImage:
    """encode_text_and_image 函数测试"""

    @patch("offline_precess_worker.bge_model")
    @patch("offline_precess_worker.clip_model")
    def test_encode_text_only(self, mock_clip, mock_bge):
        """纯文本编码，无图片"""
        mock_bge.encode.return_value = np.zeros(512)
        mock_clip.encode.return_value = np.zeros(1024)

        text_bge, text_clip, img_clip = encode_text_and_image(
            "这是一段纯文本",
            "/path/to/doc.md"
        )

        assert len(text_bge) == 512
        assert len(text_clip) == 1024
        assert len(img_clip) == 1024
        assert all(v == 0 for v in img_clip)

    @patch("offline_precess_worker.os.path.exists")
    @patch("offline_precess_worker.bge_model")
    @patch("offline_precess_worker.clip_model")
    def test_encode_with_image(self, mock_clip, mock_bge, mock_exists):
        """包含图片的文本编码"""
        mock_exists.return_value = True
        mock_bge.encode.return_value = np.ones(512)
        mock_clip.encode.return_value = np.ones(1024)

        text = "文本内容\n![图1](./images/fig1.png)"
        text_bge, text_clip, img_clip = encode_text_and_image(
            text,
            "/data/docs/paper/vlm/content.md"
        )

        assert len(text_bge) == 512
        assert len(text_clip) == 1024
        assert len(img_clip) == 1024
        assert not all(v == 0 for v in img_clip)

    @patch("offline_precess_worker.bge_model")
    @patch("offline_precess_worker.clip_model")
    def test_encode_bge_exception_handling(self, mock_clip, mock_bge):
        """bge编码异常时返回零向量"""
        mock_bge.encode.side_effect = Exception("model error")
        mock_clip.encode.return_value = np.zeros(1024)

        text_bge, text_clip, img_clip = encode_text_and_image(
            "测试文本",
            "/path/to/doc.md"
        )

        assert len(text_bge) == 512
        assert all(v == 0 for v in text_bge)

    @patch("offline_precess_worker.os.path.exists")
    @patch("offline_precess_worker.bge_model")
    @patch("offline_precess_worker.clip_model")
    def test_image_path_construction(self, mock_clip, mock_bge, mock_exists):
        """图片路径拼接逻辑验证"""
        mock_exists.return_value = True
        mock_bge.encode.return_value = np.zeros(512)
        mock_clip.encode.return_value = np.zeros(1024)

        text = "![图1](./images/fig1.png)"
        encode_text_and_image(text, "/data/docs/paper/vlm/content.md")

        call_args = mock_clip.encode.call_args_list
        image_calls = [c for c in call_args if "fig1.png" in str(c)]
        assert len(image_calls) > 0
```

---

### 4.4 test_unit/test_orm.py

```python
import pytest
from orm_model import File


class TestFileORM:
    """File ORM模型测试"""

    def test_create_file_record(self, db_session):
        """创建文件记录"""
        file = File(
            filename="test.pdf",
            filepath="uploads/abc123.pdf",
            filestate="已上传"
        )
        db_session.add(file)
        db_session.commit()

        assert file.id is not None
        assert file.filename == "test.pdf"
        assert file.filepath == "uploads/abc123.pdf"
        assert file.filestate == "已上传"

    def test_query_file(self, db_session):
        """查询文件记录"""
        file = File(filename="test.pdf", filepath="uploads/abc.pdf", filestate="已上传")
        db_session.add(file)
        db_session.commit()

        result = db_session.query(File).filter(File.id == file.id).first()
        assert result is not None
        assert result.filename == "test.pdf"

    def test_update_file_state(self, db_session):
        """更新文件状态"""
        file = File(filename="test.pdf", filepath="uploads/abc.pdf", filestate="已上传")
        db_session.add(file)
        db_session.commit()

        file.filestate = "已解析"
        db_session.commit()

        result = db_session.query(File).filter(File.id == file.id).first()
        assert result.filestate == "已解析"

    def test_delete_file_record(self, db_session):
        """删除文件记录"""
        file = File(filename="test.pdf", filepath="uploads/abc.pdf", filestate="已上传")
        db_session.add(file)
        db_session.commit()
        file_id = file.id

        db_session.delete(file)
        db_session.commit()

        result = db_session.query(File).filter(File.id == file_id).first()
        assert result is None

    def test_query_all_files(self, db_session):
        """查询所有文件"""
        for i in range(3):
            file = File(
                filename=f"test{i}.pdf",
                filepath=f"uploads/abc{i}.pdf",
                filestate="已上传"
            )
            db_session.add(file)
        db_session.commit()

        results = db_session.query(File).all()
        assert len(results) == 3

    def test_file_state_transitions(self, db_session):
        """文件状态流转：已上传 -> 解析中 -> 已解析"""
        file = File(filename="test.pdf", filepath="uploads/abc.pdf", filestate="已上传")
        db_session.add(file)
        db_session.commit()

        file.filestate = "解析中"
        db_session.commit()
        assert file.filestate == "解析中"

        file.filestate = "已解析"
        db_session.commit()
        assert file.filestate == "已解析"

    def test_file_state_failed(self, db_session):
        """文件解析失败状态"""
        file = File(filename="test.pdf", filepath="uploads/abc.pdf", filestate="已上传")
        db_session.add(file)
        db_session.commit()

        file.filestate = "解析失败"
        db_session.commit()
        assert file.filestate == "解析失败"
```

---

### 4.5 test_api/test_upload.py

```python
import pytest
import os
from unittest.mock import patch, Mock


class TestUploadDocument:
    """POST /upload/document 接口测试"""

    def test_upload_pdf_success(self, client, sample_pdf, mock_kafka):
        """正常上传PDF文件"""
        response = client.post(
            "/upload/document",
            files={"file": sample_pdf}
        )

        assert response.status_code == 200
        data = response.json()
        assert data["code"] == 200
        assert data["data"]["file_id"] is not None
        assert data["data"]["filename"] == "test.pdf"
        assert data["data"]["filestate"] == "已上传"

        # 验证文件已保存
        filepath = data["data"]["filepath"]
        assert os.path.exists(filepath)

        # 验证Kafka消息发送
        mock_kafka.send.assert_called_once()
        call_args = mock_kafka.send.call_args
        assert call_args[1]["value"]["file_name"] == "test.pdf"

    def test_upload_docx_success(self, client, sample_docx, mock_kafka):
        """正常上传DOCX文件"""
        response = client.post(
            "/upload/document",
            files={"file": sample_docx}
        )

        assert response.status_code == 200
        assert response.json()["data"]["filename"] == "test.docx"

    def test_upload_txt_success(self, client, sample_txt, mock_kafka):
        """正常上传TXT文件"""
        response = client.post(
            "/upload/document",
            files={"file": sample_txt}
        )

        assert response.status_code == 200
        assert response.json()["data"]["filename"] == "test.txt"

    def test_upload_unsupported_type(self, client):
        """上传不支持的文件类型"""
        response = client.post(
            "/upload/document",
            files={"file": ("test.exe", b"content", "application/octet-stream")}
        )

        assert response.status_code == 400

    def test_upload_empty_file(self, client):
        """上传空文件"""
        response = client.post(
            "/upload/document",
            files={"file": ("empty.pdf", b"", "application/pdf")}
        )

        assert response.status_code == 400

    def test_upload_no_file_field(self, client):
        """请求中无file字段"""
        response = client.post("/upload/document")

        assert response.status_code == 422

    def test_upload_kafka_message_format(self, client, sample_pdf, mock_kafka):
        """验证Kafka消息格式"""
        response = client.post(
            "/upload/document",
            files={"file": sample_pdf}
        )

        file_id = response.json()["data"]["file_id"]

        mock_kafka.send.assert_called_once()
        call_args = mock_kafka.send.call_args
        kafka_msg = call_args[1]["value"]

        assert "file_name" in kafka_msg
        assert "file_path" in kafka_msg
        assert "id" in kafka_msg
        assert kafka_msg["id"] == file_id

    def test_upload_uuid_filename(self, client, sample_pdf, mock_kafka):
        """验证文件名使用UUID"""
        response = client.post(
            "/upload/document",
            files={"file": sample_pdf}
        )

        filepath = response.json()["data"]["filepath"]
        filename = os.path.basename(filepath)

        # UUID格式: 8-4-4-4-12
        name_without_ext = filename.split(".")[0]
        assert len(name_without_ext) == 36
        assert name_without_ext.count("-") == 4
```

---

### 4.6 test_api/test_chat.py

```python
import pytest
from unittest.mock import patch, Mock


class TestChat:
    """POST /chat 接口测试"""

    def test_chat_success(self, client, mock_bge_model, mock_milvus, mock_qwen_client):
        """正常问答"""
        mock_milvus.search.return_value = [[
            {
                "entity": {
                    "text": "RAG是检索增强生成的缩写",
                    "db_id": 1,
                    "file_name": "test.pdf",
                    "file_path": "uploads/abc.pdf"
                },
                "distance": 0.85
            },
            {
                "entity": {
                    "text": "RAG结合了检索和生成两种技术",
                    "db_id": 1,
                    "file_name": "test.pdf",
                    "file_path": "uploads/abc.pdf"
                },
                "distance": 0.78
            }
        ]]

        response = client.post(
            "/chat",
            json={"question": "什么是RAG？"}
        )

        assert response.status_code == 200
        data = response.json()
        assert data["code"] == 200
        assert "answer" in data["data"]
        assert "sources" in data["data"]
        assert len(data["data"]["sources"]) > 0

    def test_chat_empty_question(self, client):
        """空问题"""
        response = client.post("/chat", json={"question": ""})

        assert response.status_code == 400

    def test_chat_missing_question_field(self, client):
        """缺少question字段"""
        response = client.post("/chat", json={})

        assert response.status_code == 422

    def test_chat_no_search_results(self, client, mock_bge_model, mock_milvus, mock_qwen_client):
        """Milvus无匹配结果"""
        mock_milvus.search.return_value = [[]]

        response = client.post(
            "/chat",
            json={"question": "完全无关的内容xyz123"}
        )

        assert response.status_code == 200
        data = response.json()
        assert data["data"]["sources"] == []

    def test_chat_search_params(self, client, mock_bge_model, mock_milvus, mock_qwen_client):
        """验证Milvus检索参数"""
        mock_milvus.search.return_value = [[]]

        client.post("/chat", json={"question": "测试问题"})

        mock_milvus.search.assert_called_once()
        call_kwargs = mock_milvus.search.call_args[1]
        assert call_kwargs["collection_name"] == "rag_data_new"
        assert call_kwargs["limit"] == 5
        assert call_kwargs["anns_field"] == "text_vector"

    def test_chat_prompt_construction(self, client, mock_bge_model, mock_milvus, mock_qwen_client):
        """验证Prompt拼接"""
        mock_milvus.search.return_value = [[
            {
                "entity": {
                    "text": "相关资料内容",
                    "db_id": 1,
                    "file_name": "test.pdf",
                    "file_path": "uploads/abc.pdf"
                },
                "distance": 0.85
            }
        ]]

        client.post("/chat", json={"question": "测试问题"})

        mock_qwen_client.chat.completions.create.assert_called_once()
        call_kwargs = mock_qwen_client.chat.completions.create.call_args[1]
        messages = call_kwargs["messages"]

        user_msg = [m for m in messages if m["role"] == "user"][0]
        assert "测试问题" in user_msg["content"]
        assert "相关资料内容" in user_msg["content"]

    def test_chat_image_path_replacement(self, client, mock_bge_model, mock_milvus, mock_qwen_client):
        """验证图片路径替换逻辑"""
        mock_milvus.search.return_value = [[
            {
                "entity": {
                    "text": "内容\n![图1](./images/fig1.png)",
                    "db_id": 1,
                    "file_name": "test.pdf",
                    "file_path": "uploads/abc123.pdf"
                },
                "distance": 0.85
            }
        ]]

        response = client.post("/chat", json={"question": "查看图片"})

        assert response.status_code == 200

    def test_chat_multiple_results(self, client, mock_bge_model, mock_milvus, mock_qwen_client):
        """验证多条检索结果拼接"""
        mock_milvus.search.return_value = [[
            {
                "entity": {
                    "text": f"资料片段{i}",
                    "db_id": 1,
                    "file_name": "test.pdf",
                    "file_path": "uploads/abc.pdf"
                },
                "distance": 0.9 - i * 0.1
            }
            for i in range(5)
        ]]

        response = client.post("/chat", json={"question": "测试问题"})

        assert response.status_code == 200
        call_kwargs = mock_qwen_client.chat.completions.create.call_args[1]
        user_msg = call_kwargs["messages"][1]["content"]
        for i in range(5):
            assert f"资料片段{i}" in user_msg

    def test_chat_bge_encode_called(self, client, mock_bge_model, mock_milvus, mock_qwen_client):
        """验证bge编码被调用"""
        mock_milvus.search.return_value = [[]]

        client.post("/chat", json={"question": "测试问题"})

        mock_bge_model.encode.assert_called_once_with("测试问题", normalize_embeddings=True)

    def test_chat_llm_exception_handling(self, client, mock_bge_model, mock_milvus):
        """LLM调用异常处理"""
        mock_milvus.search.return_value = [[]]

        with patch("main.qwen_client") as mock_qwen:
            mock_qwen.chat.completions.create.side_effect = Exception("API Error")
            response = client.post("/chat", json={"question": "测试问题"})

        assert response.status_code in [200, 500]
```

---

### 4.7 test_api/test_files.py

```python
import pytest
import os
from unittest.mock import patch


class TestListFiles:
    """GET /files 接口测试"""

    def test_list_files_empty(self, client):
        """无文件时返回空列表"""
        response = client.get("/files")

        assert response.status_code == 200
        data = response.json()
        assert data["code"] == 200
        assert data["data"] == []

    def test_list_files_with_data(self, client, sample_pdf, mock_kafka):
        """有文件时返回列表"""
        client.post("/upload/document", files={"file": sample_pdf})

        response = client.get("/files")

        assert response.status_code == 200
        data = response.json()
        assert len(data["data"]) == 1
        assert data["data"][0]["filename"] == "test.pdf"

    def test_list_files_multiple(self, client, mock_kafka):
        """多个文件"""
        for i in range(3):
            client.post(
                "/upload/document",
                files={"file": (f"test{i}.pdf", b"content", "application/pdf")}
            )

        response = client.get("/files")

        assert response.status_code == 200
        assert len(response.json()["data"]) == 3

    def test_list_files_fields(self, client, sample_pdf, mock_kafka):
        """验证返回字段完整性"""
        client.post("/upload/document", files={"file": sample_pdf})

        response = client.get("/files")
        file_data = response.json()["data"][0]

        assert "id" in file_data
        assert "filename" in file_data
        assert "filepath" in file_data
        assert "filestate" in file_data


class TestDeleteFile:
    """DELETE /files/{file_id} 接口测试"""

    def test_delete_success(self, client, sample_pdf, mock_kafka, mock_milvus):
        """删除存在的文件"""
        upload_resp = client.post("/upload/document", files={"file": sample_pdf})
        file_id = upload_resp.json()["data"]["file_id"]
        filepath = upload_resp.json()["data"]["filepath"]

        response = client.delete(f"/files/{file_id}")

        assert response.status_code == 200

        list_resp = client.get("/files")
        assert len(list_resp.json()["data"]) == 0

        mock_milvus.delete.assert_called_once()
        call_kwargs = mock_milvus.delete.call_args[1]
        assert call_kwargs["collection_name"] == "rag_data_new"

    def test_delete_not_found(self, client):
        """删除不存在的文件"""
        response = client.delete("/files/99999")

        assert response.status_code == 404

    def test_delete_nonexistent_local_file(self, client, db_session, mock_milvus):
        """本地文件不存在时也能删除记录"""
        from orm_model import File

        file = File(
            filename="ghost.pdf",
            filepath="uploads/nonexistent.pdf",
            filestate="已上传"
        )
        db_session.add(file)
        db_session.commit()
        file_id = file.id

        response = client.delete(f"/files/{file_id}")
        assert response.status_code == 200

    def test_delete_milvus_cleanup(self, client, sample_pdf, mock_kafka, mock_milvus):
        """验证Milvus清理参数"""
        upload_resp = client.post("/upload/document", files={"file": sample_pdf})
        file_id = upload_resp.json()["data"]["file_id"]

        client.delete(f"/files/{file_id}")

        mock_milvus.delete.assert_called_once()
        call_kwargs = mock_milvus.delete.call_args[1]
        assert "db_id" in str(call_kwargs) or "filter" in call_kwargs

    def test_delete_twice(self, client, sample_pdf, mock_kafka, mock_milvus):
        """重复删除同一文件"""
        upload_resp = client.post("/upload/document", files={"file": sample_pdf})
        file_id = upload_resp.json()["data"]["file_id"]

        response1 = client.delete(f"/files/{file_id}")
        assert response1.status_code == 200

        response2 = client.delete(f"/files/{file_id}")
        assert response2.status_code == 404
```

---

### 4.8 test_integration/test_full_flow.py

```python
import pytest
import os
import concurrent.futures
from unittest.mock import patch, Mock


class TestFullFlow:
    """完整业务流程集成测试"""

    def test_upload_to_search_flow(self, client, mock_kafka, mock_milvus,
                                    mock_bge_model, mock_qwen_client, mock_mineru_result):
        """上传 -> 检索 完整流程"""
        # 1. 上传文件
        upload_resp = client.post(
            "/upload/document",
            files={"file": ("test.pdf", b"pdf content", "application/pdf")}
        )
        assert upload_resp.status_code == 200
        file_id = upload_resp.json()["data"]["file_id"]
        assert upload_resp.json()["data"]["filestate"] == "已上传"

        # 2. 验证文件列表
        list_resp = client.get("/files")
        assert len(list_resp.json()["data"]) == 1
        assert list_resp.json()["data"][0]["id"] == file_id

        # 3. Mock Milvus有数据
        mock_milvus.search.return_value = [[
            {
                "entity": {
                    "text": "测试文档内容",
                    "db_id": file_id,
                    "file_name": "test.pdf",
                    "file_path": "uploads/abc.pdf"
                },
                "distance": 0.9
            }
        ]]

        # 4. 提问
        chat_resp = client.post("/chat", json={"question": "测试文档说了什么？"})
        assert chat_resp.status_code == 200
        assert "answer" in chat_resp.json()["data"]

    def test_upload_delete_search_flow(self, client, mock_kafka, mock_milvus,
                                        mock_bge_model, mock_qwen_client):
        """上传 -> 删除 -> 检索 流程"""
        # 1. 上传
        upload_resp = client.post(
            "/upload/document",
            files={"file": ("test.pdf", b"pdf content", "application/pdf")}
        )
        file_id = upload_resp.json()["data"]["file_id"]

        # 2. 删除
        delete_resp = client.delete(f"/files/{file_id}")
        assert delete_resp.status_code == 200

        # 3. 验证文件列表为空
        list_resp = client.get("/files")
        assert len(list_resp.json()["data"]) == 0

        # 4. 提问，验证搜不到已删除文件
        mock_milvus.search.return_value = [[]]

        chat_resp = client.post("/chat", json={"question": "测试文档内容"})
        assert chat_resp.status_code == 200
        assert chat_resp.json()["data"]["sources"] == []

    def test_multi_file_search(self, client, mock_kafka, mock_milvus,
                                 mock_bge_model, mock_qwen_client):
        """多文件上传 -> 跨文件检索"""
        # 1. 上传3个文件
        file_ids = []
        for i in range(3):
            resp = client.post(
                "/upload/document",
                files={"file": (f"doc{i}.pdf", b"content", "application/pdf")}
            )
            file_ids.append(resp.json()["data"]["file_id"])

        # 2. Mock Milvus返回多个文件的结果
        mock_milvus.search.return_value = [[
            {
                "entity": {
                    "text": f"文档{i}的内容",
                    "db_id": file_ids[i],
                    "file_name": f"doc{i}.pdf",
                    "file_path": f"uploads/file{i}.pdf"
                },
                "distance": 0.9 - i * 0.1
            }
            for i in range(3)
        ]]

        # 3. 提问
        chat_resp = client.post("/chat", json={"question": "所有文档的汇总"})

        assert chat_resp.status_code == 200
        sources = chat_resp.json()["data"]["sources"]
        source_files = [s["file_name"] for s in sources]
        assert len(set(source_files)) > 1

    def test_concurrent_uploads(self, client, mock_kafka):
        """并发上传测试"""
        def upload_file(index):
            return client.post(
                "/upload/document",
                files={"file": (f"test{index}.pdf", b"content", "application/pdf")}
            )

        with concurrent.futures.ThreadPoolExecutor(max_workers=10) as executor:
            futures = [executor.submit(upload_file, i) for i in range(10)]
            results = [f.result() for f in futures]

        success_count = sum(1 for r in results if r.status_code == 200)
        assert success_count == 10

        list_resp = client.get("/files")
        assert len(list_resp.json()["data"]) == 10
```

---

## 五、测试运行配置

### 5.1 pytest.ini

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: 单元测试
    api: 接口测试
    integration: 集成测试
```

### 5.2 运行命令

```bash
# 运行所有测试
pytest

# 运行单元测试
pytest tests/test_unit/ -m unit

# 运行接口测试
pytest tests/test_api/ -m api

# 运行集成测试
pytest tests/test_integration/ -m integration

# 运行并生成覆盖率报告
pytest --cov=. --cov-report=html
```

### 5.3 Mock数据

```python
# 预置的markdown内容（用于mock mineru解析结果）
MOCK_MARKDOWN = """
# 测试文档标题

## 第一章

这是第一章的内容，介绍RAG的基本概念。

![RAG架构图](./images/rag_arch.png)

## 第二章

这是第二章的内容，介绍向量检索的原理。
"""

# 预置的Milvus搜索结果
MOCK_MILVUS_RESULTS = [
    {
        "entity": {
            "text": "RAG是检索增强生成的缩写",
            "db_id": 1,
            "file_name": "test.pdf",
            "file_path": "uploads/abc.pdf"
        },
        "distance": 0.85
    },
]

# 预置的Qwen-VL回答
MOCK_LLM_ANSWER = "根据资料显示，RAG（检索增强生成）是一种..."
```
