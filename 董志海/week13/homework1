总结 V3 相比 V2 改进？


1. 架构层面的关键创新
V3 延续了 V2 已验证的 MLA（多头潜在注意力）和 DeepSeekMoE 架构，并在此基础上进行了两项关键创新：

首创无辅助损失的负载均衡策略：V2 依赖辅助损失来鼓励专家负载均衡，这可能损害模型性能。V3 则首创了一种无辅助损失的动态策略，通过为每个专家引入一个动态调整的偏置项来实现负载均衡，避免了性能下降。实验证明，该策略比纯辅助损失方法取得了更好的性能。

引入多令牌预测训练目标：V3 新增了多令牌预测（MTP）目标，让模型在每个位置不仅预测下一个令牌，还要预测后续的多个令牌。这能增强训练信号密度，提升数据效率。实验表明该策略能持续提升模型在多数评测基准上的表现，并且这些预测模块在推理时可被丢弃，不影响主模型，也可用于推测解码加速推理。

2. 训练基础设施与效率的极致优化
为实现V3的高效、低成本训练，团队对训练框架进行了深度定制和优化：

首创FP8混合精度训练：V3首次在一个极大规模模型上成功验证了FP8混合精度训练框架的有效性。这相比V2的BF16训练，显著加速了训练并减少了GPU内存占用。

设计DualPipe算法：为掩盖跨节点专家并行带来的巨大通信开销，V3设计了创新的DualPipe流水线并行算法。它巧妙地重叠了前向和后向过程中的计算与通信阶段，实现了接近零的通信开销，并大幅减少了传统流水线并行中的“气泡”时间。

极致的显存优化：通过重计算RMSNorm和MLA上投影、将EMA参数存于CPU，以及在MTP模块与主模型间物理共享嵌入层和输出头参数等方法，显著降低了显存占用，使得训练此大模型无需使用昂贵的张量并行（TP）。

高效的跨节点通信内核：为充分利用InfiniBand (IB)和NVLink带宽，团队定制了高效的跨节点all-to-all通信内核，并结合网络拓扑与MoE门控算法，通过限制令牌分发节点数、实现IB与NVLink通信的完全重叠等方式，只用20个流处理器(SM)就能充分利用通信带宽。

3. 后训练流程的升级
V3 的后训练流程也变得更复杂和强大：

从长链思维模型蒸馏推理能力：后训练的一个关键步骤是从DeepSeek-R1系列模型（长链思维模型）向V3蒸馏推理能力。通过构建包含反思和验证模式的SFT数据并进行强化学习，显著提升了V3在数学、编程等领域的推理性能。

采用自我奖励机制：在通用场景中，V3利用自身作为生成式奖励模型，通过自我反馈和宪法式AI方法进行强化学习对齐，提升了主观评估的表现，实现了重要的范式扩展。

总的来说，DeepSeek-V3相对于V2的进步是系统性的。它在继承高效基础架构的同时，通过算法创新（无辅助损失负载均衡、MTP）解除了性能枷锁，通过系统工程的巅峰优化（FP8、DualPipe）实现了极致的训练效率，再通过新的后训练范式（知识蒸馏）进一步拔高了模型能力，最终在性能和成本上都取得了显著突破。
